= form_for @anuncio, :html => { class: 'form-horizontal', role: 'fordb/migrate/20131119172026_create_diseños.rbm' } do |f|
  - if @anuncio.errors.any?
    #error_explanation
      %h2= "#{pluralize(@anuncio.errors.count, "error")}"
      %ul
        - @anuncio.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :link, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :link, class: 'form-control'
  .form-group
    = f.label :imagen, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :imagen, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
