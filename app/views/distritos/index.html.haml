%h1 Listing distritos

%table
  %tr
    %th Nombre
    %th
    %th
    %th

  - @distritos.each do |distrito|
    %tr
      %td= distrito.nombre
      %td= link_to 'Show', distrito
      %td= link_to 'Edit', edit_distrito_path(distrito)
      %td= link_to 'Destroy', distrito, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Distrito', new_distrito_path
