- page_title!('Editando publicacion')

#region-publicaciones.container

  .region
    .region-title
      %h1
        Editando
        %b
          publicacion

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @publicacion, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
