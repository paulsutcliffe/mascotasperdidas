- page_title!('Editando plan')

#region-planes.container

  .region
    .region-title
      %h1
        Editando
        %b
          plan

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @plan, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
