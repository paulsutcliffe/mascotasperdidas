%h1 Listing planes

= link_to 'Nuevo Plan', new_plan_path

%table
  %tr
    %th Dias
    %th Precio
    %th Descripcion
    %th
    %th
    %th

  - @planes.each do |plan|
    %tr
      %td= plan.dias
      %td= plan.precio
      %td= plan.descripcion
      %td= link_to 'Ver mas', plan
      %td= link_to 'Editar', edit_plan_path(plan)
      %td= link_to 'Eliminar', plan, :method => :delete, :data => { :confirm => '¿Estas Seguro?' }

%br

