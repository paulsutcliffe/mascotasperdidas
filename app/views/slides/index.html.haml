%h1 Slides

= link_to 'Nuevo Slide', new_slide_path

%table
  %tr
    %th Link
    %th Imagen
    %th
    %th

  - @slides.each do |slide|
    %tr
      %td= slide.link
      %td= image_tag slide.imagen_url(:full)
      %td= link_to 'Editar', edit_slide_path(slide)
      %td= link_to 'Eliminar', slide, :method => :delete, :data => { :confirm => '¿Estas Seguro?' }

%br

