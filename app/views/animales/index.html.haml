%h1 Animales

= link_to 'Nuevo Animal', new_animal_path

%table
  %tr
    %th Nombre
    %th
    %th
    %th

  - @animales.each do |animal|
    %tr
      %td= animal.nombre
      %td= link_to 'Editar', edit_animal_path(animal)
      %td= link_to 'Eliminar', animal, :method => :delete, :data => { :confirm => '¿Estas Seguro?' }

%br

