- page_title!('Editando portada')

#region-portadas.container

  .region
    .region-title
      %h1
        Creando
        %b
          portada

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
